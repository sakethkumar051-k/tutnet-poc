# Backend API base URL (must end with /api or the code will append it)
VITE_API_URL=http://localhost:5001/api

# Razorpay test key (publishable — safe to expose; key ID only, never secret)
# Server passes its own key in the order-create response, so this is only a fallback.
VITE_RAZORPAY_KEY_ID=
